import { ComponentWithProps } from './tsUtils';

export const DEFAULT_INPUT: ComponentWithProps<'input'> = {
  component: 'input',
};
export const DEFAULT_LABEL: ComponentWithProps<'label'> = {
  component: 'label',
};
